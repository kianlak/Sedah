export interface AmbientFieldConfig {
  dotColor: string;
  opacity: number;
}

export interface AmbientFieldProfile {
  amplitudePx: number;
  spacingPx: number;
  speedSeconds: number;
  dotSizePx: number;
  perspective: number;
}
