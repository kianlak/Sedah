# Middleware

This folder is for cross-cutting pipeline behavior shared across services or local adapters.

Examples:

- auth guards
- request tracing
- structured error translation
- retry and resilience helpers
- shared transport middleware
